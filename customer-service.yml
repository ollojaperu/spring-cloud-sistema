server:
   port: ${PORT:8861}
message: Configuracion de cliente en el servidor levantado!
spring:
  application:
    name: customer-service
  datasource:
    url: jdbc:mysql://localhost:3306/cloudcrud?serverTimezone=UTC
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update   
  cloud:
    config:
      uri: http://localhost:8860 
 
